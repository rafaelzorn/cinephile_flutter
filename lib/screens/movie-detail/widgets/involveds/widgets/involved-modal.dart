import 'package:flutter/material.dart';

class InvolvedModalWidget extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Container(
      height: (MediaQuery.of(context).size.height * 0.7),
      child: Container(),
    );
  }
}
